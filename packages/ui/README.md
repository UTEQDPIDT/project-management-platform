# Component Library

The components used in this library are from [shadcn](https://ui.shadcn.com/docs).

To add a specific component from shadcn use the following command on the terminal:

```bash
pnpm dlx shadcn@latest add [component]
```
